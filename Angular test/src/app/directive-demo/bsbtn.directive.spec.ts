import { BsbtnDirective } from './bsbtn.directive';

describe('BsbtnDirective', () => {
  it('should create an instance', () => {
    const directive = new BsbtnDirective();
    expect(directive).toBeTruthy();
  });
});
